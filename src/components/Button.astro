---
const { color = "text-4xl font-bold bg-gradient-to-r from-pink-200 via-pink-400 to-fuchsia-600 bg-clip-text text-transparent", url = "", newtab = false, As = "a", ...props } = Astro.props;
---

<As
	class={`not-prose button button-${color}`}
	href={url}
	target={newtab ? "_blank" : "_self"}
	rel={newtab ? "noopener noreferrer" : ""}
	{...props}
>
	<slot />
</As>
